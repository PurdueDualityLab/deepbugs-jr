name: github-ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git checkout develop
      - run: python3 src/deepbugs-jr.py
  test:
    runs-on: ubuntu-latest
    steps:
      - run: actions/checkout@v2
      - run: git checkout develop
      - run: python -m unittest
